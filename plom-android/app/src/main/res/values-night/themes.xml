<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Plom" parent="Theme.AppCompat">
        <item name="android:windowLightStatusBar" tools:targetApi="35">false</item>
    </style>

    <style name="Theme.Plom.NoActionBar" parent="Theme.Plom">
        <!-- I'm not sure if the next two items are supposed to automatically inherit from the
         default theme or not, but it seems to not do so for me, so I'll redefine them -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="android:windowLightStatusBar" tools:targetApi="35">false</item>
    </style>
</resources>